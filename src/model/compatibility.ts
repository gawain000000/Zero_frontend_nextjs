// ---------- 初始化 ----------

// ---------- 开发区间 ----------
export interface CompatibilityQueryInfo {
  platform: string
  item_type: string
  category: string
  platform_id?: string
  date?: string
}
